<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.inzynierka">

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.tripPlans.editTrip.TripEditActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".ui.tripPlans.addTrip.AddTripActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".ui.tripPlans.selectTripPlan.TripPlanSelectActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".ui.articles.articlesList.article.ArticleActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".ui.articles.articlesList.ArticlesActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".StartActivity"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>